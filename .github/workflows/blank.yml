name: CI

on:
  workflow_dispatch:

jobs:
          
  test2:
    runs-on: ubuntu-latest
    
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: test_artifact
      - shell: bash
        run: |
          value=`cat artifact.txt`
          echo $value
